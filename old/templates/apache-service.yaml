apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-apache-helm.fullname" . }}-apache
  labels:
    {{- include "nginx-apache-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nginx-apache-helm.selectorLabels" . | nindent 4 }}
