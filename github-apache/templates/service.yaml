apiVersion: v1
kind: Service
metadata:
  name: {{ include "github-helm.fullname" . }}
  labels:
    {{- include "github-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "github-helm.selectorLabels" . | nindent 4 }}
