apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "github-helm.fullname" . }}-test-connection"
  labels:
    {{- include "github-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "github-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
