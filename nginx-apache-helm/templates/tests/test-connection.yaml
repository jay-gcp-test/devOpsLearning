apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginx-apache-helm.fullname" . }}-test-connection"
  labels:
    {{- include "nginx-apache-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nginx-apache-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
